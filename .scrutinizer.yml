build:
    tests:
        override:
            -
                command: 'php bin/console d:d:c -e=test && bin/phpunit --coverage-clover=coverage'
                coverage:
                    file: 'coverage'
                    format: 'clover'
