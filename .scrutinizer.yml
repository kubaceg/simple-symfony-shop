build:
    tests:
        override:
            -
                command: 'bin/phpunit --coverage-clover=some-file'
                coverage:
                    file: 'some-file'
                    format: 'clover'
